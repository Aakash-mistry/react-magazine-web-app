import { makeStyles, createStyles } from '@material-ui/styles'

export default makeStyles(() => {
     return createStyles(
          {
               nodata: {
                    textAlign: 'center',
                    color: '#000',
                    background: '#f5f5f5',
               }
          }
     )
})