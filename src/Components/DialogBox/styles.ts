import { makeStyles, createStyles } from '@material-ui/core'

export default makeStyles(() => {
     return createStyles({
          listItemtext: {
               boxShadow: '0',
               margin: 0,
               padding: 0,
               border: 'none',
               marginTop: 0,
               cursor: 'link'
          }
     })
})